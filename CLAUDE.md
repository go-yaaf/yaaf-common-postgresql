# CLAUDE.md

See @AGENTS.md for full guidance on this repository (architecture, build/test
commands, conventions, and the security invariants that must be preserved when
editing SQL/query construction code).

That file is the single source of truth for AI coding assistants — keep it and this
pointer in sync if the guidance changes.
